export enum Type {
  ATTACK = 'attack',
  SKILL = 'skill',
  POWER = 'power'
}

export enum Target {
  PLAYER = 'player',
  ENEMY = 'enemy',
  GAME = 'game'
}

export enum Phase {
  COMBAT = 'combat',
  REWARD = 'reward'
}